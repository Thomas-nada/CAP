// Environment configuration — keep this file out of version control.
// Copy from env.example.js and fill in your values.

// Local development: paste your GitHub PAT here.
// In production (Render), leave this as null — users log in via the Setup screen.
export const GITHUB_TOKEN = null;
